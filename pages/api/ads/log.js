import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL||'', process.env.SUPABASE_SERVICE_ROLE_KEY||''); export default async function handler(req,res){ if(req.method!=='POST') return res.status(405).end(); const { userId, adId, event } = req.body || {}; try{ await supabase.from('ads_events').insert([{ user_id: userId, ad_id: adId, event_type: event }]); }catch(e){} res.json({ ok:true }); }
